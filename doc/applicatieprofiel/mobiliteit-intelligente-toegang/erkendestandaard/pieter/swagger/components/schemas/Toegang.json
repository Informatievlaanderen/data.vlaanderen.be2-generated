{
  "title": "Toegang",
  "type": "object",
  "description": "Recht om een bepaalde zone binnen te mogen.",
  "properties": {
    "@id": {
      "type": "string",
      "format": "uri"
    },
    "@type": {
      "type": "string",
      "description": "Object type (klasse Toegang)",
      "pattern": "^Toegang$"
    },
    "periode": {
      "$ref": "#/components/schemas/Periode"
    },
    "type": {
      "$ref": "#/components/schemas/Toegangstype"
    },
    "voorwerp": {
      "type": "array",
      "description": "Lijst van Activiteit items.",
      "items": {
        "$ref": "#/components/schemas/Activiteit"
      },
      "minItems": 1
    },
    "voertuig": {
      "$ref": "#/components/schemas/Voertuig"
    }
  },
  "required": [
    "@type",
    "periode",
    "type"
  ]
}