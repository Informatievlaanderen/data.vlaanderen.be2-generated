{
  "title": "Zone",
  "type": "object",
  "description": "Een geografische gebied waar een beperking is op de toegang.",
  "properties": {
    "@id": {
      "type": "string",
      "format": "uri"
    },
    "@type": {
      "type": "string",
      "description": "Object type (klasse Zone)",
      "pattern": "^Zone$"
    },
    "naam": {
      "type": "object",
      "description": "Naam van de Zone.",
      "properties": {
        "@value": {
          "type": "string"
        }
      },
      "required": [
        "@value"
      ]
    },
    "geometrie": {
      "$ref": "#/components/schemas/Geometrie"
    }
  },
  "required": [
    "@type",
    "naam",
    "geometrie"
  ]
}