{
  "title": "Address",
  "type": "object",
  "description": "Information that makes it possible to refer in a structured and unique way to a building unit, a berth, a location or a plot on the basis of a municipality name, a street name, a house number and possibly a bus number and a postal code.",
  "properties": {
    "@id": {
      "type": "string",
      "format": "uri"
    },
    "@type": {
      "type": "string",
      "description": "Object type (klasse Address)",
      "pattern": "^Address$"
    }
  },
  "required": [
    "@type"
  ]
}