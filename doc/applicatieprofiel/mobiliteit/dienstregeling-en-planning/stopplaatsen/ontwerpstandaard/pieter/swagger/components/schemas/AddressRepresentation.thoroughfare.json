{
  "title": "AddressRepresentation.thoroughfare",
  "type": "object",
  "description": "An address component that represents the name of a passageway or passageway from one location to another. A thoroughfare is not necessarily a road, it might be a waterway or some other feature.",
  "properties": {
    "@value": {
      "type": "string"
    },
    "@language": {
      "type": "string",
      "pattern": "^nl$"
    }
  },
  "required": [
    "@value",
    "@language"
  ]
}