{
  "title": "Adresvoorstelling.volledigAdres",
  "type": "object",
  "description": "The complete address in one string, formatted or not. Avoids errors due to splitting an address into its parts. Displays the prescribed order of the different parts.",
  "properties": {
    "@value": {
      "type": "string"
    },
    "@language": {
      "type": "string",
      "pattern": "^nl$"
    }
  },
  "required": [
    "@value",
    "@language"
  ]
}