{
  "title": "ContactInfo",
  "type": "object",
  "description": "Information such as email, telephone, address that allows to contact someone or something.",
  "properties": {
    "@id": {
      "type": "string",
      "format": "uri"
    },
    "@type": {
      "type": "string",
      "description": "Object type (klasse ContactInfo)",
      "pattern": "^ContactInfo$"
    },
    "formOfAddress": {
      "$ref": "#/components/schemas/ContactInfo.formOfAddress"
    },
    "contactName": {
      "$ref": "#/components/schemas/ContactInfo.contactName"
    },
    "email": {
      "$ref": "#/components/schemas/ContactInfo.email"
    },
    "openingHours": {
      "$ref": "#/components/schemas/ContactInfo.openingHours"
    },
    "phone": {
      "$ref": "#/components/schemas/ContactInfo.phone"
    },
    "website": {
      "$ref": "#/components/schemas/ContactInfo.website"
    },
    "address": {
      "$ref": "#/components/schemas/Adresvoorstelling"
    },
    "availability": {
      "$ref": "#/components/schemas/OpeningHoursSpecification"
    }
  },
  "required": [
    "@type"
  ]
}