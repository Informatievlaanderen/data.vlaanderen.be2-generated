{
  "title": "GroupOfStopPlaces",
  "type": "object",
  "description": "A grouping of Stop Places which will be commonly referenced for a specific purpose (stops inside an airport, main stops of a city, etc.).",
  "properties": {
    "@id": {
      "type": "string",
      "format": "uri"
    },
    "@type": {
      "type": "string",
      "description": "Object type (klasse GroupOfStopPlaces)",
      "pattern": "^GroupOfStopPlaces$"
    },
    "name": {
      "$ref": "#/components/schemas/GroupOfStopPlaces.name"
    },
    "shortName": {
      "$ref": "#/components/schemas/GroupOfStopPlaces.shortName"
    },
    "description": {
      "$ref": "#/components/schemas/GroupOfStopPlaces.description"
    },
    "privateCode": {
      "$ref": "#/components/schemas/GroupOfStopPlaces.privateCode"
    },
    "transportMode": {
      "$ref": "#/components/schemas/Transportmode"
    },
    "purposeOfGrouping": {
      "$ref": "#/components/schemas/Code"
    },
    "submode": {
      "$ref": "#/components/schemas/Transportmode"
    },
    "member": {
      "type": "array",
      "description": "Stop Place that belongs to the Group of Stop Places. Lijst van StopPlace items.",
      "items": {
        "$ref": "#/components/schemas/StopPlace"
      },
      "minItems": 1
    }
  },
  "required": [
    "@type"
  ]
}