{
  "title": "Level",
  "type": "object",
  "description": "An identified storey (ground, first, basement, mezzanine, etc) within an interchange building or Site on which Site Components reside.",
  "properties": {
    "@id": {
      "type": "string",
      "format": "uri"
    },
    "@type": {
      "type": "string",
      "description": "Object type (klasse Level)",
      "pattern": "^Level$"
    },
    "name": {
      "$ref": "#/components/schemas/Level.name"
    },
    "shortName": {
      "$ref": "#/components/schemas/Level.shortName"
    },
    "description": {
      "$ref": "#/components/schemas/Level.description"
    },
    "publicUse": {
      "$ref": "#/components/schemas/Level.publicUse"
    },
    "publicCode": {
      "$ref": "#/components/schemas/Level.publicCode"
    },
    "allAreasWheelchair": {
      "$ref": "#/components/schemas/Level.allAreasWheelchair"
    },
    "accessibliltyAssessment": {
      "$ref": "#/components/schemas/AccessibilityAssessment"
    }
  },
  "required": [
    "@type"
  ]
}