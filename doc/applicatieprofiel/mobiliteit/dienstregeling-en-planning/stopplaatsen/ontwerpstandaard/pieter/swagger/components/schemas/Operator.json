{
  "title": "Operator",
  "type": "object",
  "description": "A company providing public transport services.",
  "properties": {
    "@id": {
      "type": "string",
      "format": "uri"
    },
    "@type": {
      "type": "string",
      "description": "Object type (klasse Operator)",
      "pattern": "^Operator$"
    },
    "country": {
      "$ref": "#/components/schemas/Operator.country"
    },
    "address": {
      "$ref": "#/components/schemas/AddressRepresentation"
    },
    "primaryMode": {
      "$ref": "#/components/schemas/Transportmode"
    },
    "alternativeName": {
      "type": "array",
      "description": "Other name by which the organization is known. For example commercial name, informal name…. Lijst van LanguageString items.",
      "items": {
        "$ref": "#/components/schemas/Operator.alternativeName"
      },
      "minItems": 1
    },
    "description": {
      "$ref": "#/components/schemas/Operator.description"
    },
    "preferredName": {
      "$ref": "#/components/schemas/Operator.preferredName"
    },
    "contactInfo": {
      "type": "array",
      "description": "Information such as email, telephone... that allows the Organisation to be contacted. Lijst van ContactInfo items.",
      "items": {
        "$ref": "#/components/schemas/ContactInfo"
      },
      "minItems": 1
    },
    "type": {
      "type": "array",
      "description": "Type of organization. Specialization of classification (besides activity & status). Lijst van OrganizationType items.",
      "items": {
        "$ref": "#/components/schemas/OrganizationType"
      },
      "minItems": 1
    }
  },
  "required": [
    "@type",
    "preferredName"
  ]
}