{
  "title": "Polygon",
  "type": "object",
  "description": "Area consisting of 1 outer boundary and 0 or more internal boundaries. Each inner border forms a hole in the polygon.",
  "properties": {
    "@id": {
      "type": "string",
      "format": "uri"
    },
    "@type": {
      "type": "string",
      "description": "Object type (klasse Polygon)",
      "pattern": "^Polygon$"
    },
    "gml": {
      "$ref": "#/components/schemas/Polygon.gml"
    },
    "wKT": {
      "$ref": "#/components/schemas/Polygon.wKT"
    }
  },
  "required": [
    "@type"
  ]
}