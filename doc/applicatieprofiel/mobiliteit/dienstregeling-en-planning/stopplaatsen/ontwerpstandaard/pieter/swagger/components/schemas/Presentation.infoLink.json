{
  "title": "Presentation.infoLink",
  "type": "object",
  "description": "URL of an image associated with the entity. For example an icon.",
  "properties": {
    "@id": {
      "type": "string",
      "format": "uri"
    },
    "@type": {
      "type": "string",
      "pattern": "^AnyURI$"
    }
  },
  "required": [
    "@id",
    "@type"
  ]
}