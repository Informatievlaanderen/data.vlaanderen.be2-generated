{
  "title": "StopPlace.alternativeName",
  "type": "object",
  "description": "Alternative name for the Stop Place. Used for Stops that have multiple aliases.",
  "properties": {
    "@value": {
      "type": "string"
    },
    "@language": {
      "type": "string",
      "pattern": "^nl$"
    }
  },
  "required": [
    "@value",
    "@language"
  ]
}