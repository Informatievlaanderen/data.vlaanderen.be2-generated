{
  "title": "UicOperationelePeriode.totDatum",
  "type": "object",
  "description": "Einde operationele dag (inclusief).",
  "properties": {
    "@value": {
      "type": "string",
      "format": "date-time"
    },
    "@type": {
      "type": "string",
      "pattern": "^DateTime$"
    }
  },
  "required": [
    "@value",
    "@type"
  ]
}