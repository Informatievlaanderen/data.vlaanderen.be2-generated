{
  "title": "UicOperationelePeriode.vanDatum",
  "type": "object",
  "description": "Start operationele dag (inclusief).",
  "properties": {
    "@value": {
      "type": "string",
      "format": "date-time"
    },
    "@type": {
      "type": "string",
      "pattern": "^DateTime$"
    }
  },
  "required": [
    "@value",
    "@type"
  ]
}