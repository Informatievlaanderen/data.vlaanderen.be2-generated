{
  "title": "Accessibility",
  "type": "object",
  "description": "The extent to which an infrastructure is open or accessible.",
  "properties": {
    "@id": {
      "type": "string",
      "format": "uri"
    },
    "@type": {
      "type": "string",
      "description": "Object type (klasse Accessibility)",
      "pattern": "^Accessibility$"
    },
    "level": {
      "$ref": "#/components/schemas/AccessibilityLevel"
    },
    "part": {
      "type": "array",
      "description": "Part of the service or infrastructure to which the Accessibility relates. Lijst van AccessibilityComponent items.",
      "items": {
        "$ref": "#/components/schemas/AccessibilityComponent"
      },
      "minItems": 1
    }
  },
  "required": [
    "@type"
  ]
}