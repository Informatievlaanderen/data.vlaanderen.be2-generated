{
  "title": "AddressPresentation.fullAddress",
  "type": "object",
  "description": "The complete address in one string, formatted or not. Avoid errors due to the splitting of the address into its parts. Displays the prescribed order of the different parts.",
  "properties": {
    "@value": {
      "type": "string"
    },
    "@language": {
      "type": "string",
      "pattern": "^nl$"
    }
  },
  "required": [
    "@value",
    "@language"
  ]
}