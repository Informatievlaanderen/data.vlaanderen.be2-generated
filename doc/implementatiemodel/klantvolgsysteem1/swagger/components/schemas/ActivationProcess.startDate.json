{
  "title": "ActivationProcess.startDate",
  "type": "object",
  "description": "The date on which the Trajectory starts.",
  "properties": {
    "@value": {
      "type": "string",
      "format": "date-time"
    },
    "@type": {
      "type": "string",
      "pattern": "^DateTime$"
    }
  },
  "required": [
    "@value",
    "@type"
  ]
}