{
  "title": "Identifier.grantedOn",
  "type": "object",
  "description": "Time when the identifier was issued.",
  "properties": {
    "@value": {
      "type": "string",
      "format": "date-time"
    },
    "@type": {
      "type": "string",
      "pattern": "^DateTime$"
    }
  },
  "required": [
    "@value",
    "@type"
  ]
}