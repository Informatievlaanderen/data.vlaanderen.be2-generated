{
  "title": "PartialTrajectory.endDate",
  "type": "object",
  "description": "The date on which the Trajectory ends.",
  "properties": {
    "@value": {
      "type": "string",
      "format": "date-time"
    },
    "@type": {
      "type": "string",
      "pattern": "^DateTime$"
    }
  },
  "required": [
    "@value",
    "@type"
  ]
}