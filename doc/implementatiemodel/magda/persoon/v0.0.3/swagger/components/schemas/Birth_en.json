{
  "title": "Birth",
  "type": "object",
  "description": "The coming into the world of a Person.",
  "properties": {
    "@id": {
      "type": "string",
      "format": "uri"
    },
    "@type": {
      "type": "string",
      "description": "Object type (klasse Birth)",
      "pattern": "^Birth$"
    },
    "date": {
      "$ref": "#/components/schemas/Birth.date"
    },
    "location": {
      "oneOf": [
        {
          "$ref": "#/components/schemas/Location"
        },
        {
          "$ref": "#/components/schemas/AdministrativeUnit"
        }
      ],
      "discriminator": {
        "propertyName": "@type",
        "mapping": {
          "Location": "#/components/schemas/Location",
          "AdministrativeUnit": "#/components/schemas/AdministrativeUnit"
        }
      }
    }
  },
  "required": [
    "@type"
  ]
}